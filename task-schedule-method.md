# 任務時間分配決策系統 — 規格文件

版本：v1.1
用途：定義任務標籤系統的正式資料模型、一致性規則、衍生指標與決策邏輯，作為後續開發（如工具、應用程式、自動化排程）的基礎文件。

---

## 1. 文件目的與設計原則

前一版本（標籤組合速查表）的問題在於：邏輯衝突只用文字提醒，沒有形成可執行的規則；36 種組合是窮舉列出，彼此之間沒有推導關係，難以維護與擴充。

本版本改用以下設計原則：

1. **符號需有正式定義**，每個標籤對應唯一、明確的條件。
2. **邏輯衝突用「正規化規則」解決**，而不是用警示語提醒使用者自己判斷。
3. **「何時處理」與「如何處理」拆成兩個獨立維度**分別推導，避免用窮舉表格硬記 36 種狀況。
4. **所有原始組合都必須可追溯**：任何一組 (A,B,C,D) 標籤，都能透過固定步驟算出對應策略，而非查表硬記。

---

## 2. 符號與定義

### 2.1 維度 A — 期限類型（Deadline Type）

| 符號 | 定義 | 判準 |
|---|---|---|
| **A1** | 硬期限 | 存在外部具體後果的截止日（他人在等待、有金錢/資格損失、無法逆轉的時間點） |
| **A2** | 軟期限 | 自己設定的目標日期，未達成無外部後果 |
| **A3** | 無期限 | 沒有任何具體日期 |

### 2.2 維度 B — 任務型態（Task Nature）

| 符號 | 定義 | 判準 |
|---|---|---|
| **B1** | 累積型 | 需要多次、長期投入才能產生效果，單次投入無法完成 |
| **B2** | 一次性型 | 完成一次決策/行動後即結束，不需要持續投入 |

### 2.3 維度 C — 依賴關係（Dependency）

| 符號 | 定義 | 判準 |
|---|---|---|
| **C1** | 連鎖依賴 | 此任務未完成會阻塞至少一個其他任務的進行 |
| **C2** | 獨立任務 | 此任務的完成與否不影響其他任務 |

### 2.4 維度 D — 主導權（Ownership）

| 符號 | 定義 | 判準 |
|---|---|---|
| **D1** | 自己主導 | 決策與執行完全由自己負責 |
| **D2** | 共同決策 | 需要與他人同步討論才能推進 |
| **D3** | 輕量參與 | 由他人主導，自己僅需知情或偶爾表態 |

### 2.5 任務物件的形式化定義

一個任務被定義為四元組：

```
Task = (a, b, c, d)
其中 a ∈ {A1, A2, A3}
     b ∈ {B1, B2}
     c ∈ {C1, C2}
     d ∈ {D1, D2, D3}
```

範例：`學習 ML = (A2, B1, C1, D1)`

---

## 3. 一致性規則（正規化規則）

前一版本用「⚠️衝突提醒」處理的狀況，在此版本改為**強制正規化規則**：輸入任務後，先套用以下規則產生「正規化後的任務」，再進行後續推導。這樣同一組原始標籤，永遠得到同一個確定的處理結果，而不是留給使用者自行判斷。

### INV-1｜連鎖依賴不可能無期限

**條件**：`c = C1` 且 `a = A3`
**問題**：會阻塞其他任務的事項，必然受下游任務的期限牽動，不可能真的沒有期限，只是期限尚未被明確化。

**正規化動作分兩層，MUST 與 MAY 明確區分**：

- **必要行為（MUST）**：任何實作都必須至少執行「降級正規化」——將 `a` 強制調整為 `A2`（視為至少有自訂期限），並將輸入欄位 `downstream_deadline` 視為未提供。這是系統的最低保證行為，沒有例外。
- **選用行為（MAY）**：實作可以額外提供「互動式追問」功能——當偵測到 `c = C1` 且 `a = A3` 時，向使用者詢問下游實際需要的日期；若使用者回填了具體日期，則將 `a' ← A1` 並記錄該日期於 `deadline_date` 欄位。若實作不提供此功能，或使用者未回答，一律回退到上面的必要行為（`a' = A2`）。

**規格意圖**：這樣設計是為了避免「同一組 A3B1C1D1 輸入，不同實作得到不同結果卻都宣稱符合規格」的問題——凡是不做互動追問的實作，行為是唯一確定的（降級為 A2）；凡是做互動追問的實作，只是在確定的基礎行為之上疊加一個明確定義、可選的加強路徑，不會產生行為分歧。

**輸出標記**：無論走哪條路徑，只要 `a ≠ a'`，此任務都必須標記 `normalized = true`，供後續追溯（見第 4.3 節輸出結構）。

### INV-2｜連鎖依賴 + 輕量參與需要角色複核

**條件**：`c = C1` 且 `d = D3`
**問題**：理論上輕量參與者不該是任務瓶頸，但也可能是使用者誤判了自己的角色。
**處理動作**：不強制修改標籤，但在輸出結果中附加**角色複核提示**：「請確認：這件事是否真的不需要你主導？若答案是『需要』，請將 D 改為 D1 或 D2 後重新計算。」

**與矩陣的關係（避免語意重複）**：第 6 節策略矩陣的 D3B1 / D3B2 欄位文字，職責僅限於「check-in 頻率」這種機械性資訊，**不得**包含任何「主動介入」「示警」「追問主責人」之類暗示使用者可能需要接手的語氣——那是 INV-2 提示的職責，不是矩陣格的職責。換句話說：**矩陣格回答「多常關注」，INV-2 回答「要不要接手」，兩者不重疊**。若實作在 UI 上同時顯示矩陣建議與 INV-2 提示，兩者應是互補而非同義重複的兩句話。

### INV-3｜累積型任務涉及他人時，需拆分時段類型

**條件**：`b = B1` 且 `d ∈ {D2, D3}`
**問題**：累積型任務本質是個人反覆投入，若同時牽涉他人步調（共同決策或輕量參與他人的累積進度），「個人投入的時段」與「跟他人同步的時段」步調不同，混在一起排程會互相干擾。

**處理動作（結構性，非附加提示）**：此規則的條件 `b = B1 且 d ∈ {D2, D3}` 恰好等於 Allocation Type 為 `D2B1` 或 `D3B1` 的條件（見 4.2 節）。因此 INV-3 **不透過 Step 4 產生獨立的文字提示**，而是直接規定：**Allocation Type 為 `D2B1` 或 `D3B1` 時，輸出格式本身必須是結構化的雙欄位物件**（例如 `{personal: "...", sync_or_watch: "..."}`），**不得**輸出成單一字串把兩件事用「+」串起來。第 6 節矩陣中這兩欄的呈現方式已依此規則調整，不再是可以自由解讀的自然語言字串。

**規格意圖**：避免「INV-3 提示」與「矩陣格文字」各自表述同一件事，導致同一份資料在畫面上被拆分邏輯呈現兩次。拆分行為只在一個地方定義（Allocation Type 的資料結構），不重複出現在提示層。

---

## 4. 衍生指標

本系統不直接查表得出策略，而是先算出兩個獨立指標，再由兩者組合決定策略。這是「何時處理」與「如何處理」的拆分：

- **U（緊急層級）**：由正規化後的 `a'` 與 `c` 決定，代表這個任務在時間軸上該多快被排入。
- **Allocation Type（投入型態）**：由 `d` 與 `b` 決定，代表實際執行時該用什麼方式切時間，與緊急與否無關。

### 4.1 緊急層級 U 的推導表

輸入：正規化後的 `a'`、原始 `c`

| a' | c | U | 說明 |
|---|---|---|---|
| A1 | C1 | **U1** | 最高緊急：硬期限且卡住他人 |
| A1 | C2 | **U2** | 高：硬期限但不卡人 |
| A2 | C1 | **U2** | 高：軟期限但卡住他人（因阻塞而升級） |
| A2 | C2 | **U3** | 中：軟期限且不卡人 |
| A3 | C2 | **U4** | 低：無期限且不卡人（唯一允許保留 A3 的情況，因為 C1+A3 已被 INV-1 正規化排除） |

> 注意：`(A3, C1)` 在正規化後不會出現在此表中，因為 INV-1 已將其轉換為 `(A2, C1) → U2`。

### 4.2 投入型態（Allocation Type）的推導

投入型態由 `D`（決定「投入方式」）與 `B`（決定「投入節奏」）組合而成，共 6 種：

| 代碼 | D | B | 名稱 | 說明 |
|---|---|---|---|---|
| **D1B1** | 自己主導 | 累積型 | 深度執行—持續型 | 需長期反覆投入大塊個人時間 |
| **D1B2** | 自己主導 | 一次性 | 深度執行—集中型 | 需要專注時段但完成後即結束 |
| **D2B1** | 共同決策 | 累積型 | 協調對齊—持續型 | 結構化輸出：`{personal, sync}` 雙欄位（依 INV-3） |
| **D2B2** | 共同決策 | 一次性 | 協調對齊—單次型 | 安排一次討論拍板即可（單一欄位） |
| **D3B1** | 輕量參與 | 累積型 | 背景關注—持續型 | 結構化輸出：`{personal, watch}` 雙欄位（依 INV-3） |
| **D3B2** | 輕量參與 | 一次性 | 背景關注—單次型 | 被動接收資訊，幾乎不需主動排時間（單一欄位） |

> `D2B1`、`D3B1` 是本系統中僅有的兩種**結構化（雙欄位）**輸出類型，其餘四種一律是單一字串。這個差異由 INV-3 規定，不是實作可自由選擇的呈現方式。

### 4.3 輸出結構定義

為滿足第 1 節設計原則「所有原始組合都必須可追溯」，最終輸出**必須**包含以下欄位，而不是只有 `(U, AllocationType, 提示列表)` 這三個值：

```
Output = {
  a_original:   原始輸入的 a（正規化前）,
  a_normalized: 正規化後的 a'（若未觸發 INV-1，等於 a_original）,
  normalized:   boolean，是否曾被 INV-1 調整,
  b, c, d:      原始輸入值（不會被正規化，直接透傳）,
  U:            緊急層級 U1–U4,
  allocation_type: D1B1 / D1B2 / D2B1 / D2B2 / D3B1 / D3B2,
  strategy:     依 allocation_type 而定——
                若為 D2B1 或 D3B1：{personal, sync|watch} 物件,
                其餘：單一字串,
  hints:        提示列表，僅可能包含 "INV-2"（角色複核）；
                INV-3 不進入此欄位，因其效果已體現在 strategy 的結構中
}
```

**追溯保證**：只要保留 `a_original` 與 `a_normalized` 兩個欄位，任何下游系統都能重建「這個任務是否被正規化、原始標籤是什麼」，不會遺失第 3 節所說的追溯資訊。

---

## 5. 決策演算法

```
輸入：Task(a, b, c, d)

Step 1. 正規化（INV-1）
  a_original ← a
  若 (c = C1 且 a = A3)：
      【MAY，選用】若實作提供互動追問，且使用者回填 downstream_deadline：
          a' ← A1；記錄 deadline_date ← downstream_deadline
      【MUST，必要，其餘所有情況的預設路徑】否則：
          a' ← A2
  否則：a' ← a
  normalized ← (a' ≠ a_original)

Step 2. 計算緊急層級 U
  依 4.1 推導表，用 (a', c) 查出 U ∈ {U1, U2, U3, U4}

Step 3. 計算投入型態與策略內容
  依 4.2 推導表，用 (d, b) 查出 allocation_type
  依第 6 節矩陣，用 (U, allocation_type) 查出 strategy
    若 allocation_type ∈ {D2B1, D3B1}：strategy 為雙欄位物件
    否則：strategy 為單一字串

Step 4. 附加提示（僅 INV-2，INV-3 已於 Step 3 體現在 strategy 結構中，不重複輸出）
  hints ← []
  若 (c = C1 且 d = D3)：hints.append("INV-2：請複核是否真的不需要主導")

輸出：Output = { a_original, a_normalized: a', normalized, b, c, d, U, allocation_type, strategy, hints }
（完整欄位定義見 4.3 節）
```

---

## 6. 策略矩陣（U × Allocation Type）

任何任務經過上述演算法後，會落在下方矩陣的其中一格，這一格就是最終的時間分配建議。矩陣共 4 × 6 = 24 格，涵蓋所有正規化後可能出現的狀態。

**格式規則**：`D2B1`、`D3B1` 兩欄依 INV-3 規定為結構化雙欄位（表格中以「個人 / 同步」「個人 / 關注」兩行呈現，對應輸出結構的 `{personal, sync}` 與 `{personal, watch}`）；其餘四欄為單一字串。`D3B1`、`D3B2` 兩欄的文字**只描述 check-in 頻率等機械性資訊**，不包含任何「該不該接手」的語氣——那是 INV-2 提示的職責（見 5.1 節與下方 U1 列的備註）。

| U＼類型 | D1B1 深度-持續 | D1B2 深度-集中 | D2B1 協調-持續 | D2B2 協調-單次 | D3B1 背景-持續 | D3B2 背景-單次 |
|---|---|---|---|---|---|---|
| **U1 最高** | 每日/每週固定大塊深度時段，反向排程，不可延後 | 立即用時間盒本週內集中處理完畢 | 個人：每週固定深度時段<br>同步：每週 ≥2 次會議 | 24–48 小時內安排同步討論並拍板 | 個人：（通常無）<br>關注：最高頻 check-in（建議每日） | 每日關注最新進展 |
| **U2 高** | 每週固定 2–3 次深度時段 | 本週內安排 1–2 個時間盒完成 | 個人：每週固定時段<br>同步：每週 1 次會議 | 一週內安排一次同步討論拍板 | 個人：（通常無）<br>關注：中頻 check-in（每週 1–2 次） | 每週定期關注 |
| **U3 中** | 每週 1–2 次保底時段，可彈性調整 | 找完整零碎時段（如週末半天）處理 | 個人：隔週時段<br>同步：隔週會議 | 兩週內找機會討論拍板 | 個人：（通常無）<br>關注：低頻 check-in（每兩週） | 有需要再關注 |
| **U4 低** | 保底頻率自訂，可低至每週一次或更低 | 放入「有空清單」，填補零碎時間 | 個人：頻率自訂<br>同步：低頻、各自累積 | 隨緣安排討論 | 個人：（通常無）<br>關注：偶爾關心即可 | 完全被動，回應即可 |

**U1 列備註**：U1 恆對應 `c = C1`（見 4.1 節），因此 U1 × D3B1／U1 × D3B2 這兩格必然同時觸發 INV-2 角色複核提示。矩陣格本身只回答「多常關注」，「要不要考慮接手」這句話只會出現在 INV-2 提示裡，兩者不會在畫面上重複出現同義的兩句話。

**閱讀方式**：先用第 5 節演算法算出任務的 `(U, allocation_type)`，再對照此矩陣找到對應格子；若該格是雙欄位格式，輸出時維持兩個獨立欄位，不合併成一句話。

---

## 7. 附錄 A：完整 36 組合追溯表

此表列出所有原始標籤組合，展示每一組如何經過正規化、推導出 U 與 Allocation Type，最終對應矩陣中的哪一格。用於驗證系統邏輯，也可作為未來程式化實作的測試案例（test cases）。

| # | 原始組合 | 正規化後 a' | U | Allocation Type | 觸發規則 |
|---|---|---|---|---|---|
| 1 | A1B1C1D1 | A1 | U1 | D1B1 | — |
| 2 | A1B1C1D2 | A1 | U1 | D2B1 | INV-3 |
| 3 | A1B1C1D3 | A1 | U1 | D3B1 | INV-2, INV-3 |
| 4 | A1B1C2D1 | A1 | U2 | D1B1 | — |
| 5 | A1B1C2D2 | A1 | U2 | D2B1 | INV-3 |
| 6 | A1B1C2D3 | A1 | U2 | D3B1 | INV-3 |
| 7 | A1B2C1D1 | A1 | U1 | D1B2 | — |
| 8 | A1B2C1D2 | A1 | U1 | D2B2 | — |
| 9 | A1B2C1D3 | A1 | U1 | D3B2 | INV-2 |
| 10 | A1B2C2D1 | A1 | U2 | D1B2 | — |
| 11 | A1B2C2D2 | A1 | U2 | D2B2 | — |
| 12 | A1B2C2D3 | A1 | U2 | D3B2 | — |
| 13 | A2B1C1D1 | A2 | U2 | D1B1 | — |
| 14 | A2B1C1D2 | A2 | U2 | D2B1 | INV-3 |
| 15 | A2B1C1D3 | A2 | U2 | D3B1 | INV-2, INV-3 |
| 16 | A2B1C2D1 | A2 | U3 | D1B1 | — |
| 17 | A2B1C2D2 | A2 | U3 | D2B1 | INV-3 |
| 18 | A2B1C2D3 | A2 | U3 | D3B1 | INV-3 |
| 19 | A2B2C1D1 | A2 | U2 | D1B2 | — |
| 20 | A2B2C1D2 | A2 | U2 | D2B2 | — |
| 21 | A2B2C1D3 | A2 | U2 | D3B2 | INV-2 |
| 22 | A2B2C2D1 | A2 | U3 | D1B2 | — |
| 23 | A2B2C2D2 | A2 | U3 | D2B2 | — |
| 24 | A2B2C2D3 | A2 | U3 | D3B2 | — |
| 25 | A3B1C1D1 | **A2**（已正規化） | U2 | D1B1 | INV-1 |
| 26 | A3B1C1D2 | **A2**（已正規化） | U2 | D2B1 | INV-1, INV-3 |
| 27 | A3B1C1D3 | **A2**（已正規化） | U2 | D3B1 | INV-1, INV-2, INV-3 |
| 28 | A3B1C2D1 | A3 | U4 | D1B1 | — |
| 29 | A3B1C2D2 | A3 | U4 | D2B1 | INV-3 |
| 30 | A3B1C2D3 | A3 | U4 | D3B1 | INV-3 |
| 31 | A3B2C1D1 | **A2**（已正規化） | U2 | D1B2 | INV-1 |
| 32 | A3B2C1D2 | **A2**（已正規化） | U2 | D2B2 | INV-1 |
| 33 | A3B2C1D3 | **A2**（已正規化） | U2 | D3B2 | INV-1, INV-2 |
| 34 | A3B2C2D1 | A3 | U4 | D1B2 | — |
| 35 | A3B2C2D2 | A3 | U4 | D2B2 | — |
| 36 | A3B2C2D3 | A3 | U4 | D3B2 | — |

> **欄位說明更新（v1.1）**：「觸發規則」欄中的 INV-3 標記僅表示「該列的 `allocation_type` 屬於 D2B1 或 D3B1，因此輸出為結構化雙欄位」，**不代表**會產生獨立的提示文字——INV-3 的效果已內建於 4.2 節的資料結構定義，不進入 `hints` 欄位（見 4.3 節輸出結構）。只有 INV-2 標記才對應 `hints` 欄位中的實際提示內容。

---

## 8. 已知限制（供後續開發參考）

1. **單任務評估，非多任務排程**：本系統只回答「這個任務該怎麼分配時間」，不處理「同時有多個 U1 任務時該先做哪一個」的排序問題。若要擴充，需要在 U1 內部再加入次要排序依據（如實際剩餘天數）。
2. **未考慮個人精力/時段品質**：矩陣建議「大塊深度時段」，但沒有納入使用者一天中專注力高低的時段差異，需使用者自行對應到實際行事曆。
3. **INV-2 依賴使用者自我報告**：系統無法自動判斷使用者是否誤判自己的角色，只能提示複核，無法強制修正。
4. **正規化規則目前只處理 3 種已知衝突**：若未來發現新的邏輯矛盾組合，需要新增 INV-4、INV-5 等規則，並回頭檢查是否影響第 4、6、7 節的推導結果。

---

## 9. 版本紀錄

| 版本 | 內容 |
|---|---|
| v1.0 | 初版規格：定義符號、三條正規化規則、U/Allocation Type 兩維度推導、24 格策略矩陣、36 組合完整追溯表 |
| v1.1 | 修正審查發現的 4 項規格缺口：<br>①INV-1 明確區分 MUST（降級為 A2）與 MAY（互動追問升級為 A1）兩條路徑，避免不同實作行為分歧；<br>②INV-3 改為結構化輸出定義（見 4.2、4.3 節），不再與矩陣格文字的「+」寫法互相打架；<br>③新增 4.3 節輸出結構，補回 `a_original` / `a_normalized` / `normalized` 等追溯欄位；<br>④矩陣 D3B1/D3B2 欄位移除升級語氣，與 INV-2 提示分工，避免語意重複 |